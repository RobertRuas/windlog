---
kind: external_dependency
name: React + Vite Frontend Application
slug: react-vite-frontend
category: external_dependency
category_hints:
    - vendor_identity
scope:
    - '**'
---

Modern React 19 frontend built with Vite 6 and TypeScript. Uses @vitejs/plugin-react for JSX transformation. Development server runs on port 5173 by default. Build process compiles TypeScript and optimizes assets for production deployment.